import React from 'react';
import { Container } from '@mui/material';

const Details = () => {
    return (
        <Container>
            <h1>Cases</h1>
        </Container>
    );
};

export default Details;